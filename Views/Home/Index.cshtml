@{
    ViewData["Title"] = "Steed Bowling";
}

@model IndexViewModel

<div class="row">
    <div class="col">
        <h1 class="display-4">Welcome</h1>
        <p>Bowler Registry:</p>
    </div>
    <div class="col">
        <div page-model="@Model.PagingInfo" page-action="Index" page-classes-enabled="true"
             page-class="btn"
             page-url-teamid="@Model.CurrentTeamID"
             page-class-normal="btn-outline-secondary"
             page-class-selected="btn btn-outline-primary btn-outline-dark bg-info"
             class="btn-group pull-right m-1 pagination ">
        </div>
        <div class="row">
            <vc:navigation-menu />
            @*<div><p class="text-center">@Model.CurrentTeamName</p></div>*@
            @*<form asp-page="/AddToCart" method="get">
                <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" />
                <span class="card-text p-1">
                    <button type="submit" class="btn-success btn-sm text-nowrap pull-right float-right">
                        View cart
                    </button>
                </span>
            </form>*@
        </div>
    </div>

</div>
<div class="">
    <div class="justify-content-sm-center">
        <partial name="LeagueSummary" model="@Model.Bowlers" />
    </div>


</div>